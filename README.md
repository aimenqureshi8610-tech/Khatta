# Khatta

Lightweight ledger / expense-tracking web app. Built with Flask, deployed on Render.

## Stack
Python (Flask), HTML/CSS, Render

## Run locally
Install dependencies: pip install -r requirements.txt
Copy .env.example to .env and fill in values
Start the app: python app.py

## Status
Deployed prototype.
